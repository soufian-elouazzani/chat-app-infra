apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: chat-app
  labels:
    app: rabbitmq
spec:
  ports:
    - port: 5672
      targetPort: 5672
      name: amqp
    - port: 15672
      targetPort: 15672
      name: management
  selector:
    app: rabbitmq
